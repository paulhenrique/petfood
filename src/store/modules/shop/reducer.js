const INITIAL_STATE = {};

function shop(state = INITIAL_STATE, action) {
  switch (action.type) {
    case 'SET_CUSTOMER': {
      //LÓGICA
      break;

    }
    default: {
      return state;
    }
  }
}